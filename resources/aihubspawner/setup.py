from setuptools import setup

setup(name='aihubspawner',
      version='0.1',
      description='MLHub DockerSpawner with multiple options to set',
      url='https://github.com/ai-chain/ai-hub',
      author='AIChain Team',
      packages=['aihubspawner'],
      zip_safe=False)
